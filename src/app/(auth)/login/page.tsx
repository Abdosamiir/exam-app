import Reusable from "@/app/_components/auth/Reusable";

const page = () => {
  return (
    <div className=" grow container mx-auto flex justify-between items-center">
      {/* right side  */}
      <Reusable />

      {/* form */}
    </div>
  );
};

export default page;
